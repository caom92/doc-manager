include globals.pug

div.row
  div.col.s6
    +button(
      'button',
      'add-supplier',
      '{{ langManager.messages.inventory.guarantee.addSupplierButton }}',
      'mdi mdi-plus'
    )(
      '(click)'='onAddSupplierButtonClick()'
    )
div.row
  div.col.s12
    +table(
      'langManager.messages.inventory.guarantee.labTableHeaders'
    )
      tr('*ngFor'='let row of suppliers')
        td='{{ row.name }}'